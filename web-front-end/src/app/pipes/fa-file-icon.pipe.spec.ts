import { FaFileIconPipe } from './fa-file-icon.pipe';

describe('FaFileIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FaFileIconPipe();
    expect(pipe).toBeTruthy();
  });
});
